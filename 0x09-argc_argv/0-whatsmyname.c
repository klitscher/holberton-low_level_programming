#include "holberton.h"
#include <stdio.h>

/**
 * main - Program that prints its name
 * @argc: number of arguments passed to the program
 * @argv: a one-dimensional arrray consisting of the arguments from argc
 *
 * Return: 0
 */
int main(__attribute__ ((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
